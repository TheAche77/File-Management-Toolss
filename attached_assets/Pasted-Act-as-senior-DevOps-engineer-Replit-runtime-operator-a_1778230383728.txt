Act as senior DevOps engineer, Replit runtime operator, and full-stack deployment lead for the Street Level Discovery / SLG Growth Engine repo.

CONTEXT

Repo:
- GitHub: TheAche77/File-Management-Toolss
- Branch: crea
- Latest expected commit: 1c1d757 Add real GeoNames local dump enrichment
- Stack:
  - pnpm monorepo
  - Express + TypeScript API
  - React/Vite frontend
  - Drizzle + PostgreSQL
  - OpenAPI/generated clients
- Current repo already has:
  - `.replit`
  - Replit modules: nodejs-24, postgresql-16
  - local ports configured
  - API package: `@workspace/api-server`
  - frontend package: `@workspace/gallery-map`
  - DB migrations in `lib/db/migrations`
  - required env:
    - DATABASE_URL
    - ADMIN_API_TOKEN
    - PORT
    - BASE_PATH
    - VITE_API_PROXY_TARGET
  - enrichment:
    - Wikidata connector and preflight
    - GeoNames local dump enrichment
    - no Docker required

GOAL

Prepare the repo to run reliably in Replit.

Do not add product features.
Do not redesign UI.
Do not change domain logic.
Do not add Docker.
Do not add Overture.
Do not change enrichment behavior unless required for Replit runtime.
Do not run destructive DB commands.
Do not commit secrets.

Your task is to make Replit onboarding/deployment deterministic.

PHASE 0 — AUDIT CURRENT REPLIT READINESS

Run and inspect:

- git status --short --branch
- git log --oneline -5
- cat .replit
- cat package.json
- cat artifacts/api-server/package.json
- cat artifacts/gallery-map/package.json
- cat .env.example
- cat DEPLOY_CHECKLIST.md
- cat README.md
- cat lib/db/drizzle.config.ts
- inspect API startup:
  - artifacts/api-server/src/index.ts
- inspect frontend Vite config:
  - artifacts/gallery-map/vite.config.ts

Confirm:
- current `.replit` modules and ports
- whether `.replit` has runnable workflows or only partial config
- whether Replit PostgreSQL module exposes `DATABASE_URL` automatically
- whether API can serve frontend static build or whether Replit needs two dev processes
- whether `PORT` conflicts between API/frontend
- whether migrations are manual or can be part of setup workflow
- whether secrets are documented, not committed

PHASE 1 — DECIDE REPLIT RUNTIME STRATEGY

Use the simplest reliable Replit setup:

Preferred option:
- API runs on `PORT=8080`
- frontend runs on Vite dev server in development only
- Replit production/deployment should run API server and serve built frontend static files if the API already supports it or can be minimally configured.

If API does not currently serve frontend static files:
- do not redesign
- either:
  1. add minimal static serving from `artifacts/gallery-map/dist/public` in API production mode, or
  2. document two-process Replit development workflow
- choose the smallest safe option and explain why.

Do not introduce a new framework.

PHASE 2 — REPLIT CONFIGURATION

Update `.replit` only if needed.

Target:
- reproducible install/build/run workflow
- no Docker
- no local Homebrew assumptions
- PostgreSQL 16 module compatible
- correct port exposure
- clear development workflow

Add or adjust Replit workflows if appropriate:
- Install dependencies:
  `pnpm install`
- Run migrations:
  `pnpm --filter @workspace/db run migrate`
- Build:
  `pnpm run typecheck`
  `pnpm --filter @workspace/api-server build`
  `pnpm --filter @workspace/gallery-map build`
- Start API:
  `PORT=8080 pnpm --filter @workspace/api-server start`
- Start frontend dev, if separate:
  `PORT=19001 BASE_PATH=/ VITE_API_PROXY_TARGET=http://localhost:8080 pnpm --filter @workspace/gallery-map dev`

Do not put real tokens into `.replit`.

PHASE 3 — ENV / SECRETS RUNBOOK

Update docs so Replit setup is explicit.

Document required Replit Secrets:
- DATABASE_URL
- ADMIN_API_TOKEN
- optional SLG_USER_AGENT
- optional ENABLE_WIKIDATA_ENRICHMENT
- optional OPENCORPORATES_API_TOKEN, still disabled unless configured

Document recommended values:
- PORT=8080 for API
- BASE_PATH=/
- VITE_API_PROXY_TARGET=http://localhost:8080 for frontend dev

Clarify:
- `.env.example` is not a secret file
- Replit Secrets should be used for production-like runtime
- migrations must be run before first API start
- GeoNames dump is not committed; if needed, upload `data/cities15000.txt` manually or skip GeoNames import

PHASE 4 — VERIFICATION WITHOUT DESTRUCTIVE ACTIONS

Run locally if possible:

- pnpm run typecheck
- pnpm --filter @workspace/api-server build
- pnpm --filter @workspace/gallery-map build
- git diff --check

Do not run Docker.
Do not reset DB.
Do not require GeoNames full dump.
Do not run Wikidata as a hard blocker.

If Replit-specific commands cannot be executed locally:
- document exact commands to run inside Replit Shell.

PHASE 5 — DOCS

Update:
- README.md
- DEPLOY_CHECKLIST.md
- ROADMAP_SLG_GROWTH_ENGINE.md if needed

Add a compact section:

“Running on Replit”

Must include:
1. import repo from GitHub
2. select branch `crea`
3. confirm Replit modules:
   - nodejs-24
   - postgresql-16
4. run `pnpm install`
5. set Replit Secrets
6. run migrations
7. run API
8. run frontend dev if needed
9. smoke test:
   - /api/healthz
   - /businesses
   - /research
   - /admin
10. how to run GeoNames import if `data/cities15000.txt` is uploaded

PHASE 6 — COMMIT/PUSH

If files changed:
- git status
- git diff --stat
- commit:
  `Prepare Replit runtime setup`
- push:
  `git push origin crea`

FINAL OUTPUT

Report:

1. Current commit and git status
2. Existing Replit config status
3. Runtime strategy chosen
4. Files changed
5. Replit secrets required
6. Commands to run in Replit Shell
7. Checks run
8. Commit hash and push status
9. One next action

DEFINITION OF DONE

Done only if:
- Replit setup path is documented and deterministic
- no secrets are committed
- no Docker is introduced
- local/Homebrew assumptions are not required for Replit
- DB migrations path is clear
- API/frontend run strategy is clear
- typecheck/build pass
- working tree is clean after commit/push