{
  "name": "workspace",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "preinstall": "sh -c 'rm -f package-lock.json yarn.lock; case \"$npm_config_user_agent:$npm_execpath\" in pnpm/*:*|*:*/pnpm*) ;; *) echo \"Use pnpm instead\" >&2; exit 1 ;; esac'",
    "build": "pnpm run typecheck && pnpm -r --if-present run build",
    "enrich:pending": "pnpm --filter @workspace/api-server enrich:pending",
    "enrich:business": "pnpm --filter @workspace/api-server enrich:business --",
    "enrich:source": "pnpm --filter @workspace/api-server enrich:source --",
    "verify:wikidata-enrichment": "node scripts/verify-wikidata-enrichment.mjs",
    "mock:enrich-all": "node scripts/mock-enrich-all.mjs",
    "smoke:slg-runtime": "node scripts/smoke-slg-runtime.mjs",
    "typecheck:libs": "tsc --build",
    "typecheck": "pnpm run typecheck:libs && pnpm -r --filter \"./artifacts/**\" --filter \"./scripts\" --if-present run typecheck"
  },
  "private": true,
  "devDependencies": {
    "prettier": "^3.8.1",
    "typescript": "~5.9.2"
  },
  "dependencies": {
    "pg": "^8.20.0"
  }
}
